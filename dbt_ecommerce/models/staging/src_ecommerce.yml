version: 2

sources:
  - name: ecommerce_raw
    database: ECOMMERCE_RAW_DB
    schema: LANDING_ZONE
    tables:
      - name: DIM_CUSTOMERS    
      - name: DIM_PRODUCTS    
      - name: DIM_DATES       
      - name: FACT_SALES       